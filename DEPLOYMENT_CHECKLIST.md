# ✅ Чеклист для деплоя бота "Завхоз"

Используйте этот чеклист перед деплоем и после него для проверки работоспособности.

## 📋 Перед деплоем

### 1. Подготовка окружения
- [ ] Установлен Docker и Docker Compose (версия 3.8+)
- [ ] Проверена доступность порта 5432 (PostgreSQL)
- [ ] Создан файл `.env` на основе `.env.example`
- [ ] Все переменные в `.env` заполнены корректными значениями

### 2. Конфигурация `.env`
- [ ] `BOT_TOKEN` - токен бота от @BotFather
- [ ] `WAREHOUSEMAN_ID` - Telegram ID завхоза (проверить через @userinfobot)
- [ ] `MANAGER_ID` - Telegram ID руководителя
- [ ] `ALLOWED_EMPLOYEE_IDS` - список ID сотрудников через запятую (опционально)
- [ ] `TIMEZONE` - часовой пояс (по умолчанию Europe/Moscow)
- [ ] `LOG_LEVEL` - уровень логирования (INFO для продакшена)

### 3. Проверка кода
- [ ] Все зависимости указаны в `requirements.txt`
- [ ] Миграции Alembic готовы (файлы в `alembic/versions/`)
- [ ] Нет синтаксических ошибок в коде
- [ ] `.dockerignore` настроен правильно

### 4. База данных
- [ ] `docker-compose.yml` содержит корректный `DATABASE_URL`
- [ ] Пароли и учетные данные БД безопасны
- [ ] Если используется внешняя БД - проверена доступность

## 🚀 Деплой

### 1. Запуск контейнеров
```bash
# Сборка и запуск
docker compose up -d

# Проверка статуса
docker compose ps

# Просмотр логов
docker compose logs -f bot
```

### 2. Проверка миграций
- [ ] Миграции применены успешно (проверить в логах)
- [ ] Все таблицы созданы в БД
- [ ] Таблица `allowed_users` существует (если используется)

### 3. Проверка запуска бота
- [ ] Контейнер `zavhoz_bot` в статусе "Up"
- [ ] Контейнер `zavhoz_postgres` в статусе "Up"
- [ ] В логах нет критических ошибок
- [ ] Бот отвечает на команду `/start`

## ✅ После деплоя

### 1. Функциональная проверка
- [ ] Бот отвечает на `/start` для всех ролей
- [ ] Сотрудник может создать заявку
- [ ] Завхоз видит новые заявки
- [ ] Руководитель видит отчеты
- [ ] Белый список работает (проверить доступ)

### 2. Роли и права
- [ ] Завхоз имеет доступ ко всем функциям завхоза
- [ ] Руководитель имеет доступ ко всем функциям руководителя
- [ ] Сотрудники видят только свои функции
- [ ] Неавторизованные пользователи получают сообщение об отказе

### 3. База данных
- [ ] Заявки сохраняются в БД
- [ ] Фото заявок корректно хранятся
- [ ] Номера заявок уникальны
- [ ] Жалобы сохраняются и доступны руководителю

### 4. Уведомления
- [ ] Завхоз получает уведомления о новых заявках
- [ ] Руководитель получает уведомления о жалобах
- [ ] Фото пересылаются завхозу
- [ ] Отображаются ФИО и username отправителей

### 5. Мониторинг
- [ ] Логи сохраняются в `./logs/`
- [ ] Нет ошибок в логах
- [ ] Производительность в норме
- [ ] БД не перегружена

## 🔧 Команды для управления

```bash
# Остановка
docker compose down

# Остановка с удалением данных
docker compose down -v

# Перезапуск
docker compose restart bot

# Просмотр логов
docker compose logs -f bot
docker compose logs -f postgres

# Вход в контейнер бота
docker exec -it zavhoz_bot bash

# Применение миграций вручную
docker exec -it zavhoz_bot python -m alembic upgrade head

# Очистка базы данных (осторожно!)
docker exec -it zavhoz_bot python scripts/clear_database.py
```

## 🐛 Решение проблем

### Бот не запускается
1. Проверить логи: `docker compose logs bot`
2. Проверить переменные окружения в `.env`
3. Проверить доступность БД
4. Проверить токен бота

### База данных недоступна
1. Проверить статус: `docker compose ps`
2. Проверить логи: `docker compose logs postgres`
3. Проверить порт 5432: `netstat -an | grep 5432`
4. Перезапустить: `docker compose restart postgres`

### Миграции не применяются
1. Проверить логи бота
2. Применить вручную: `docker exec -it zavhoz_bot python -m alembic upgrade head`
3. Проверить версию: `docker exec -it zavhoz_bot python -m alembic current`

### Пользователи не имеют доступа
1. Проверить `ALLOWED_EMPLOYEE_IDS` в `.env`
2. Проверить таблицу `allowed_users` в БД
3. Проверить `WAREHOUSEMAN_ID` и `MANAGER_ID`
4. Синхронизировать: `docker exec -it zavhoz_bot python scripts/sync_allowed_users.py`

## 📞 Контакты и поддержка

При возникновении проблем:
1. Проверить логи бота
2. Проверить документацию в `docs/deployment/`
3. Связаться с разработчиком

