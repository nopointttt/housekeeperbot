# 🐳 Инструкция по деплою через Docker

Полная инструкция по развертыванию бота "Завхоз" на стационарном компьютере с использованием Docker и Docker Compose.

## 📋 Требования

- **Docker** версии 20.10 или выше
- **Docker Compose** версии 2.0 или выше
- **Windows 10/11** или **Linux** (Ubuntu/Debian)

### Установка Docker

#### Windows
1. Скачайте [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop/)
2. Установите и перезагрузите компьютер
3. Запустите Docker Desktop

#### Linux (Ubuntu/Debian)
```bash
# Обновляем пакеты
sudo apt update

# Устанавливаем Docker
sudo apt install docker.io docker-compose-plugin -y

# Добавляем пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER

# Перелогиниваемся или выполняем:
newgrp docker

# Проверяем установку
docker --version
docker compose version
```

## 🚀 Быстрый старт

### 1. Подготовка проекта

```bash
# Клонируем или копируем проект
cd /path/to/project

# Копируем пример файла окружения
cp .env.example .env
```

### 2. Настройка переменных окружения

Откройте файл `.env` и заполните необходимые переменные:

```env
# Telegram Bot Token (получить у @BotFather в Telegram)
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Telegram ID завхоза
WAREHOUSEMAN_ID=468845503

# Telegram ID руководителя
MANAGER_ID=460275491

# Список Telegram ID сотрудников (через запятую, без пробелов)
# Опционально - если не указан, используется таблица allowed_users в БД
ALLOWED_EMPLOYEE_IDS=123456789,987654321,555555555

# Часовой пояс (по умолчанию Europe/Moscow)
TIMEZONE=Europe/Moscow

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
```

**Как получить BOT_TOKEN:**
1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Скопируйте токен в `.env`

**Как узнать Telegram ID:**
1. Откройте Telegram и найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте команду `/start`
3. Бот вернет ваш ID (число, например: `123456789`)
4. Скопируйте ID в `.env`

### 3. Запуск проекта

```bash
# Собираем и запускаем контейнеры
docker compose up -d

# Просмотр логов
docker compose logs -f bot
```

### 4. Проверка работы

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением

## 📝 Управление контейнерами

### Просмотр статуса
```bash
docker compose ps
```

### Просмотр логов
```bash
# Все логи
docker compose logs

# Логи только бота
docker compose logs -f bot

# Логи только базы данных
docker compose logs -f postgres

# Последние 100 строк
docker compose logs --tail=100 bot
```

### Остановка
```bash
# Остановить контейнеры (данные сохраняются)
docker compose stop

# Остановить и удалить контейнеры (данные сохраняются)
docker compose down

# Остановить и удалить контейнеры + volumes (ВНИМАНИЕ: удалит все данные!)
docker compose down -v
```

### Перезапуск
```bash
# Перезапустить контейнеры
docker compose restart

# Перезапустить только бота
docker compose restart bot
```

### Обновление кода
```bash
# Остановить контейнеры
docker compose down

# Пересобрать образы
docker compose build --no-cache

# Запустить заново
docker compose up -d
```

## 🔧 Работа с базой данных

### Подключение к базе данных

```bash
# Через Docker
docker compose exec postgres psql -U zavhoz -d zavhoz_db

# Или через локальный клиент (если установлен)
psql -h localhost -U zavhoz -d zavhoz_db
# Пароль: zavhoz_password
```

### Резервное копирование

```bash
# Создать бэкап
docker compose exec postgres pg_dump -U zavhoz zavhoz_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановить из бэкапа
docker compose exec -T postgres psql -U zavhoz zavhoz_db < backup_20250101_120000.sql
```

### Применение миграций

Миграции применяются автоматически при запуске контейнера. Если нужно применить вручную:

```bash
# Внутри контейнера бота
docker compose exec bot python -m alembic upgrade head

# Или локально (если настроено окружение)
python -m alembic upgrade head
```

## 🐛 Решение проблем

### Бот не запускается

1. **Проверьте логи:**
   ```bash
   docker compose logs bot
   ```

2. **Проверьте переменные окружения:**
   ```bash
   docker compose exec bot env | grep BOT_TOKEN
   ```

3. **Проверьте подключение к базе данных:**
   ```bash
   docker compose exec bot python -c "from bot.config import config; print(config.database_url)"
   ```

### База данных не запускается

1. **Проверьте логи:**
   ```bash
   docker compose logs postgres
   ```

2. **Проверьте, не занят ли порт 5432:**
   ```bash
   # Windows
   netstat -ano | findstr :5432
   
   # Linux
   sudo lsof -i :5432
   ```

3. **Очистите volumes и пересоздайте:**
   ```bash
   docker compose down -v
   docker compose up -d
   ```

### Ошибка "port is already allocated"

Порт 5432 уже занят другим приложением. Измените порт в `docker-compose.yml`:

```yaml
ports:
  - "5433:5432"  # Используем порт 5433 вместо 5432
```

И обновите `DATABASE_URL` в `.env`:
```env
DATABASE_URL=postgresql+asyncpg://zavhoz:zavhoz_password@postgres:5432/zavhoz_db
```

### Бот не отвечает

1. **Проверьте, что контейнер запущен:**
   ```bash
   docker compose ps
   ```

2. **Проверьте логи на ошибки:**
   ```bash
   docker compose logs --tail=50 bot
   ```

3. **Перезапустите бота:**
   ```bash
   docker compose restart bot
   ```

## 📊 Мониторинг

### Использование ресурсов
```bash
docker stats zavhoz_bot zavhoz_postgres
```

### Размер volumes
```bash
docker system df -v
```

## 🔐 Безопасность

### Рекомендации для продакшена

1. **Измените пароль базы данных** в `docker-compose.yml`:
   ```yaml
   POSTGRES_PASSWORD: ваш_сложный_пароль_здесь
   ```

2. **Не коммитьте `.env` файл** в Git (уже добавлен в `.gitignore`)

3. **Ограничьте доступ к порту PostgreSQL** (уберите `ports` из `docker-compose.yml` для продакшена)

4. **Используйте секреты Docker** для хранения токенов:
   ```bash
   echo "your_bot_token" | docker secret create bot_token -
   ```

## 📚 Дополнительная информация

- [Документация Docker](https://docs.docker.com/)
- [Документация Docker Compose](https://docs.docker.com/compose/)
- [Документация PostgreSQL](https://www.postgresql.org/docs/)

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker compose logs`
2. Убедитесь, что все переменные окружения заполнены правильно
3. Проверьте, что порты не заняты другими приложениями
4. Перезапустите контейнеры: `docker compose restart`

