# 🚀 Деплой через GitHub Actions

Автоматический деплой Telegram бота "Завхоз" через GitHub Actions на различные платформы.

## 📋 Поддерживаемые платформы

1. **Railway.app** (Рекомендуется) - простой, с бесплатным tier
2. **Render.com** - хорошая альтернатива с бесплатным tier
3. **DigitalOcean App Platform** - через GitHub integration
4. **Google Cloud Run** - через GitHub Actions

---

## 🚂 Вариант 1: Railway.app (Рекомендуется)

### Преимущества:
- ✅ Бесплатный tier ($5 кредитов/месяц)
- ✅ Простая интеграция с GitHub
- ✅ Автоматический деплой при push в main
- ✅ Built-in PostgreSQL
- ✅ Автоматический HTTPS

### Шаги деплоя:

#### 1. Создайте аккаунт на Railway

Перейдите на [railway.app](https://railway.app) и войдите через GitHub.

#### 2. Создайте новый проект

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите репозиторий `nopointttt/housekeeperbot`
4. Railway автоматически определит Python проект

#### 3. Настройте переменные окружения

В настройках проекта (Settings → Variables) добавьте:

```
BOT_TOKEN=your_telegram_bot_token
WAREHOUSEMAN_ID=468845503
MANAGER_ID=460275491
ALLOWED_EMPLOYEE_IDS=123456789,987654321
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
DATABASE_URL=postgresql+asyncpg://...  # Будет создан автоматически при добавлении PostgreSQL
```

#### 4. Добавьте PostgreSQL базу данных

1. В проекте Railway нажмите **"New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway создаст базу и автоматически добавит переменную `DATABASE_URL`
3. Скопируйте `DATABASE_URL` в переменные окружения

#### 5. Настройте команду запуска

В настройках сервиса (Settings → Deploy) установите:

**Start Command:**
```bash
python -m alembic upgrade head && python -m main
```

#### 6. Настройте GitHub Actions (опционально)

1. В Railway перейдите в **Settings → Network**
2. Скопируйте **Railway Token**
3. В GitHub репозитории перейдите в **Settings → Secrets and variables → Actions**
4. Добавьте секрет:
   - Name: `RAILWAY_TOKEN`
   - Value: ваш Railway Token

Теперь при каждом push в `main` бот автоматически задеплоится!

#### 7. Получите URL для webhook (опционально)

Если хотите использовать webhook вместо polling:

1. В Railway → Settings → Network → Public Networking
2. Включите **Public Networking**
3. Скопируйте **Public Domain** (например: `housekeeper-production.up.railway.app`)
4. Используйте этот URL для настройки webhook в Telegram

---

## 🎨 Вариант 2: Render.com

### Преимущества:
- ✅ Бесплатный tier (750 часов/месяц)
- ✅ Автоматический деплой при push
- ✅ Built-in PostgreSQL
- ✅ Автоматический HTTPS

### Шаги деплоя:

#### 1. Создайте аккаунт на Render

Перейдите на [render.com](https://render.com) и войдите через GitHub.

#### 2. Создайте новый Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите репозиторий `nopointttt/housekeeperbot`
3. Настройте:
   - **Name:** `housekeeper-bot`
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python -m alembic upgrade head && python -m main`
   - **Plan:** Free

#### 3. Добавьте PostgreSQL базу данных

1. В Render Dashboard нажмите **"New +"** → **"PostgreSQL"**
2. Выберите **Free** план
3. Скопируйте **Internal Database URL**
4. Добавьте его как переменную окружения `DATABASE_URL`

#### 4. Настройте переменные окружения

В настройках Web Service (Environment) добавьте:

```
BOT_TOKEN=your_telegram_bot_token
DATABASE_URL=postgresql+asyncpg://...
WAREHOUSEMAN_ID=468845503
MANAGER_ID=460275491
ALLOWED_EMPLOYEE_IDS=123456789,987654321
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

#### 5. Настройте GitHub Actions (опционально)

1. В Render → Account Settings → API Keys
2. Создайте новый **API Key**
3. Скопируйте **Service ID** из URL сервиса
4. В GitHub → Settings → Secrets добавьте:
   - `RENDER_API_KEY` - ваш API key
   - `RENDER_SERVICE_ID` - ID вашего сервиса

---

## ☁️ Вариант 3: DigitalOcean App Platform

### Преимущества:
- ✅ Хорошая интеграция с GitHub
- ✅ Managed PostgreSQL
- ✅ Автоматический деплой

### Шаги деплоя:

#### 1. Создайте аккаунт на DigitalOcean

Перейдите на [digitalocean.com](https://digitalocean.com)

#### 2. Создайте новое App

1. Перейдите в **App Platform**
2. Нажмите **"Create App"**
3. Выберите **"GitHub"** как источник
4. Подключите репозиторий `nopointttt/housekeeperbot`

#### 3. Настройте Build & Run

- **Build Command:** `pip install -r requirements.txt`
- **Run Command:** `python -m alembic upgrade head && python -m main`
- **Environment:** Python 3.11

#### 4. Добавьте PostgreSQL Database

1. В настройках App добавьте **Database** компонент
2. Выберите **PostgreSQL**
3. DigitalOcean автоматически создаст переменную `DATABASE_URL`

#### 5. Настройте переменные окружения

Добавьте в Environment Variables:

```
BOT_TOKEN=your_telegram_bot_token
WAREHOUSEMAN_ID=468845503
MANAGER_ID=460275491
ALLOWED_EMPLOYEE_IDS=123456789,987654321
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

---

## 🐳 Вариант 4: Google Cloud Run (через GitHub Actions)

### Преимущества:
- ✅ Pay-per-use pricing
- ✅ Автоматическое масштабирование
- ✅ Хорошая интеграция с GitHub Actions

### Шаги деплоя:

См. файл `.github/workflows/deploy-cloudrun.yml` (будет создан позже)

---

## 🔧 Настройка GitHub Actions Workflows

После настройки одной из платформ, добавьте соответствующий workflow:

### Для Railway:

Файл уже создан: `.github/workflows/deploy-railway.yml`

Добавьте секрет `RAILWAY_TOKEN` в GitHub → Settings → Secrets → Actions

### Для Render:

Файл уже создан: `.github/workflows/deploy-render.yml`

Добавьте секреты:
- `RENDER_API_KEY`
- `RENDER_SERVICE_ID`

---

## 📝 Проверка деплоя

После деплоя проверьте:

1. **Логи приложения:**
   - Railway: Dashboard → Deployments → View Logs
   - Render: Dashboard → Service → Logs
   - DigitalOcean: App Platform → Runtime Logs

2. **Тестирование бота:**
   - Откройте Telegram
   - Найдите вашего бота
   - Отправьте `/start`
   - Бот должен ответить

3. **Проверка базы данных:**
   - Убедитесь, что миграции применены
   - Проверьте подключение к БД

---

## 🐛 Решение проблем

### Бот не запускается

1. Проверьте логи в панели платформы
2. Убедитесь, что все переменные окружения заполнены
3. Проверьте формат `DATABASE_URL` (должен быть `postgresql+asyncpg://...`)

### Ошибка подключения к БД

1. Проверьте, что PostgreSQL база создана и запущена
2. Убедитесь, что `DATABASE_URL` правильный
3. Проверьте, что миграции применены: `python -m alembic upgrade head`

### Бот не отвечает

1. Проверьте `BOT_TOKEN` - он должен быть валидным
2. Убедитесь, что бот запущен (проверьте логи)
3. Проверьте, что контейнер не падает с ошибками

---

## 💰 Сравнение платформ

| Платформа | Бесплатный tier | PostgreSQL | Автодеплой | Сложность |
|-----------|----------------|------------|------------|-----------|
| Railway   | $5 кредитов/мес | ✅ Да      | ✅ Да      | ⭐ Легко  |
| Render    | 750 часов/мес   | ✅ Да      | ✅ Да      | ⭐ Легко  |
| DigitalOcean | Нет          | ✅ Да      | ✅ Да      | ⭐⭐ Средне |
| Cloud Run | Есть (с лимитами) | ❌ Нет   | ✅ Да      | ⭐⭐⭐ Сложно |

---

## 🎯 Рекомендация

**Для начала используйте Railway.app** - это самый простой и быстрый способ задеплоить бота с минимальными настройками.

Если Railway не подходит, попробуйте Render.com - он также прост в использовании и имеет хороший бесплатный tier.

