# 🎨 Деплой на Render.com - Пошаговая инструкция

Полная инструкция по развертыванию Telegram бота "Завхоз" на Render.com через GitHub.

## 📋 Требования

- Аккаунт на [Render.com](https://render.com) (можно войти через GitHub)
- GitHub репозиторий с кодом бота
- Telegram Bot Token от @BotFather

---

## 🚀 Шаг 1: Создание аккаунта на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"Get Started for Free"**
3. Войдите через **GitHub** (рекомендуется)
4. Подтвердите доступ к репозиториям

---

## 🗄️ Шаг 2: Создание PostgreSQL базы данных

1. В Dashboard Render нажмите **"New +"**
2. Выберите **"PostgreSQL"**
3. Настройте:
   - **Name:** `housekeeper-db` (или любое другое имя)
   - **Database:** `housekeeper_db`
   - **User:** `housekeeper_user`
   - **Region:** Выберите ближайший (например, `Frankfurt` для Европы)
   - **PostgreSQL Version:** `16` (или последняя доступная)
   - **Plan:** `Free` (для начала)
4. Нажмите **"Create Database"**
5. **Важно:** Дождитесь полного создания базы (статус "Available")

### Получение DATABASE_URL

1. После создания базы, перейдите в её настройки
2. В разделе **"Connections"** найдите **"Internal Database URL"**
3. Скопируйте URL (он будет в формате: `postgresql://user:password@host:port/dbname`)
4. **Важно:** Для aiogram нужен формат `postgresql+asyncpg://...`
   - Замените `postgresql://` на `postgresql+asyncpg://`
   - Пример: `postgresql+asyncpg://user:password@host:port/dbname`

---

## 🤖 Шаг 3: Создание Web Service для бота

### Вариант A: Через render.yaml (Рекомендуется)

1. В Dashboard Render нажмите **"New +"**
2. Выберите **"Blueprint"** (если доступно) или **"Web Service"**
3. Если выбрали **"Blueprint"**:
   - Подключите репозиторий `nopointttt/housekeeperbot`
   - Render автоматически обнаружит `render.yaml`
   - Нажмите **"Apply"**
4. Если выбрали **"Web Service"**:
   - Подключите репозиторий `nopointttt/housekeeperbot`
   - Выберите ветку `main`
   - Render автоматически определит Python проект

### Вариант B: Ручная настройка

Если `render.yaml` не работает, настройте вручную:

1. **New +** → **"Web Service"**
2. Подключите репозиторий `nopointttt/housekeeperbot`
3. Настройте:
   - **Name:** `housekeeper-bot`
   - **Environment:** `Python 3`
   - **Region:** Тот же, что и для базы данных
   - **Branch:** `main`
   - **Root Directory:** (оставьте пустым)
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python -m alembic upgrade head && uvicorn bot.web:app --host 0.0.0.0 --port $PORT`
   - **Plan:** `Free` (для начала)

---

## 🔐 Шаг 4: Настройка переменных окружения

В настройках Web Service (Environment → Environment Variables) добавьте:

### Обязательные переменные:

```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

**Как получить BOT_TOKEN:**
1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot` или `/mybots` → выберите бота → API Token
3. Скопируйте токен

```env
DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname
```

**Важно:** Используйте **Internal Database URL** из настроек PostgreSQL базы, но замените `postgresql://` на `postgresql+asyncpg://`

```env
WAREHOUSEMAN_ID=468845503
MANAGER_ID=460275491
```

**Как узнать Telegram ID:**
1. Откройте Telegram и найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте `/start`
3. Бот вернет ваш ID

### Опциональные переменные:

```env
ALLOWED_EMPLOYEE_IDS=123456789,987654321,555555555
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

**Примечание:** `ALLOWED_EMPLOYEE_IDS` - список Telegram ID сотрудников через запятую (без пробелов). Если не указан, используется таблица `allowed_users` в БД.

---

## 🔗 Шаг 5: Подключение базы данных к сервису

1. В настройках Web Service перейдите в **"Environment"**
2. Найдите раздел **"Add Environment Variable"**
3. Добавьте переменную:
   - **Key:** `DATABASE_URL`
   - **Value:** Используйте **Internal Database URL** из PostgreSQL базы
   - **Важно:** Замените `postgresql://` на `postgresql+asyncpg://`

Или используйте автоматическое подключение через `render.yaml` (если используете Blueprint).

---

## 🚀 Шаг 6: Деплой

1. После настройки всех переменных, Render автоматически начнет деплой
2. Перейдите в раздел **"Events"** или **"Logs"** чтобы следить за процессом
3. Дождитесь завершения деплоя (статус "Live")

### Что происходит при деплое:

1. Render клонирует репозиторий
2. Выполняет `pip install -r requirements.txt`
3. Применяет миграции: `python -m alembic upgrade head`
4. Запускает web сервис (он слушает `$PORT` и поднимает polling в фоне): `uvicorn bot.web:app ...`

---

## ✅ Шаг 7: Проверка работы

1. **Проверьте логи:**
   - В Dashboard → Web Service → **"Logs"**
   - Должны быть сообщения: "Запуск бота 'Завхоз'...", "База данных инициализирована", "Бот запущен"

2. **Протестируйте бота:**
   - Откройте Telegram
   - Найдите вашего бота
   - Отправьте `/start`
   - Бот должен ответить приветственным сообщением

3. **Проверьте базу данных:**
   - В логах не должно быть ошибок подключения к БД
   - Миграции должны быть применены успешно

---

## 🔄 Настройка автоматического деплоя через GitHub Actions

### Шаг 1: Получение API ключа Render

1. В Render Dashboard перейдите в **Account Settings** (иконка профиля вверху справа)
2. Перейдите в раздел **"API Keys"**
3. Нажмите **"Create API Key"**
4. Дайте имя ключу (например, `github-actions`)
5. Скопируйте ключ (он показывается только один раз!)

### Шаг 2: Получение Service ID

1. В Dashboard перейдите в ваш Web Service
2. Посмотрите на URL в браузере: `https://dashboard.render.com/web/xxxxx-xxxxx-xxxxx`
3. Последняя часть URL (`xxxxx-xxxxx-xxxxx`) - это ваш **Service ID**
4. Или в настройках сервиса → **"Info"** → **"Service ID"**

### Шаг 3: Добавление секретов в GitHub

1. Перейдите в GitHub репозиторий `nopointttt/housekeeperbot`
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Добавьте два секрета:

   **Секрет 1:**
   - **Name:** `RENDER_API_KEY`
   - **Value:** Ваш API ключ из Render

   **Секрет 2:**
   - **Name:** `RENDER_SERVICE_ID`
   - **Value:** Ваш Service ID

### Шаг 4: Проверка workflow

Workflow файл уже создан: `.github/workflows/deploy-render.yml`

Теперь при каждом push в ветку `main` будет автоматически запускаться деплой на Render!

---

## 🐛 Решение проблем

### Бот не запускается

1. **Проверьте логи:**
   - Dashboard → Web Service → **"Logs"**
   - Ищите ошибки (красные сообщения)

2. **Проверьте переменные окружения:**
   - Убедитесь, что `BOT_TOKEN` заполнен
   - Проверьте формат `DATABASE_URL` (должен быть `postgresql+asyncpg://...`)

3. **Проверьте формат DATABASE_URL:**
   ```bash
   # Правильный формат:
   postgresql+asyncpg://user:password@host:port/dbname
   
   # Неправильный формат:
   postgresql://user:password@host:port/dbname
   ```

### Ошибка подключения к базе данных

1. **Проверьте, что база данных создана и запущена:**
   - Dashboard → PostgreSQL → статус должен быть "Available"

2. **Проверьте Internal Database URL:**
   - Используйте **Internal Database URL**, а не External
   - Замените `postgresql://` на `postgresql+asyncpg://`

3. **Проверьте, что база и сервис в одном регионе:**
   - Оба должны быть в одном регионе (например, Frankfurt)

### Ошибка миграций

1. **Проверьте логи при деплое:**
   - Ищите ошибки от Alembic

2. **Попробуйте применить миграции вручную:**
   - В Render Dashboard → Web Service → **"Shell"**
   - Выполните: `python -m alembic upgrade head`

### Бот не отвечает

1. **Проверьте, что сервис запущен:**
   - Статус должен быть "Live" (зеленый)

2. **Проверьте логи на ошибки:**
   - Dashboard → Web Service → **"Logs"**

3. **Проверьте BOT_TOKEN:**
   - Убедитесь, что токен правильный и не истек

4. **Перезапустите сервис:**
   - Dashboard → Web Service → **"Manual Deploy"** → **"Deploy latest commit"**

---

## 📊 Мониторинг

### Просмотр логов

1. Dashboard → Web Service → **"Logs"**
2. Можно фильтровать по уровню (INFO, ERROR, WARNING)
3. Логи обновляются в реальном времени

### Метрики

1. Dashboard → Web Service → **"Metrics"**
2. Можно посмотреть:
   - CPU использование
   - Memory использование
   - Network traffic

### Уведомления

1. Dashboard → Account Settings → **"Notifications"**
2. Настройте уведомления о:
   - Падениях сервиса
   - Ошибках деплоя
   - Превышении лимитов

---

## 💰 Тарифы Render

### Free Plan (для начала)

- **750 часов/месяц** работы сервиса
- **PostgreSQL:** 90 дней бесплатно, потом $7/месяц
- **Автоматический деплой** при push
- **HTTPS** включен
- **Логи** доступны

### Starter Plan ($7/месяц)

- **Безлимитные часы**
- **PostgreSQL:** $7/месяц
- **Больше ресурсов** (CPU, RAM)
- **Приоритетная поддержка**

---

## 🔒 Безопасность

### Рекомендации:

1. **Не коммитьте секреты в Git:**
   - Все токены и пароли должны быть в переменных окружения Render
   - Файл `.env` уже в `.gitignore`

2. **Используйте Internal Database URL:**
   - Не используйте External URL для продакшена
   - Internal URL доступен только внутри сети Render

3. **Регулярно обновляйте зависимости:**
   - Проверяйте `requirements.txt` на уязвимости
   - Обновляйте зависимости регулярно

---

## 📚 Полезные ссылки

- [Документация Render](https://render.com/docs)
- [Render Python Guide](https://render.com/docs/deploy-python)
- [Render PostgreSQL Guide](https://render.com/docs/databases)
- [Render Environment Variables](https://render.com/docs/environment-variables)

---

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи в Render Dashboard
2. Проверьте документацию Render
3. Создайте issue в GitHub репозитории
4. Обратитесь в поддержку Render (если есть активный план)

---

**Готово!** Ваш бот теперь задеплоен на Render и будет автоматически обновляться при каждом push в `main`! 🎉

