# 🚀 Инструкция по деплою бота на VPS

## ⚠️ ВАЖНО: Почему НЕ Cloudflare?

**Cloudflare Workers/Pages НЕ подходят для Telegram бота:**
- ❌ Serverless (функции запускаются по запросу, не постоянно)
- ❌ Нет поддержки long-polling (бот должен работать 24/7)
- ❌ Ограничения по времени выполнения (10 минут максимум)

**Telegram бот требует:**
- ✅ Постоянно работающий процесс (long-polling)
- ✅ Возможность запуска фоновых задач (планировщик)
- ✅ Стабильное соединение с базой данных

---

## 🎯 Рекомендуемые платформы

### 1. Hetzner Cloud (РЕКОМЕНДУЕТСЯ) 🇩🇪
- ✅ **Цена:** от €4/мес
- ✅ **Локация:** Германия (Европа - OpenAI работает)
- ✅ **Простота:** очень простой интерфейс
- ✅ **Скорость:** отличная производительность
- 🔗 https://www.hetzner.com/cloud

### 2. DigitalOcean 🇳🇱
- ✅ **Цена:** от $6/мес
- ✅ **Локация:** Амстердам (Европа)
- ✅ **Надежность:** проверенный провайдер
- 🔗 https://www.digitalocean.com

### 3. Contabo 🇩🇪
- ✅ **Цена:** от €4/мес
- ✅ **Локация:** Германия
- ✅ **Дешево:** самый бюджетный вариант
- 🔗 https://www.contabo.com

---

## 📋 ЧТО НУЖНО ДЛЯ ДЕПЛОЯ

### 1. Аккаунт на VPS провайдере
- Зарегистрируйтесь на одном из сайтов выше
- Добавьте способ оплаты

### 2. Telegram Bot Token
- У вас уже должен быть от @BotFather

### 3. База данных Neon PostgreSQL
- У вас уже есть аккаунт
- Нужна строка подключения `DATABASE_URL`

### 4. Telegram ID для ролей
- `WAREHOUSEMAN_ID` - ID завхоза
- `MANAGER_ID` - ID руководителя
- Как получить: напишите боту @userinfobot команду `/start`

### 5. Доступ к проекту
- Все файлы должны быть загружены на сервер

---

## 🔧 ПОДГОТОВКА К ДЕПЛОЮ

### Шаг 1: Создайте файл с переменными окружения

Создайте файл `.env` (будет создан на сервере):

```env
# Telegram Bot Token
BOT_TOKEN=ваш_токен_от_BotFather

# База данных Neon PostgreSQL
DATABASE_URL=postgresql+asyncpg://user:password@host/database

# Telegram IDs для ролей
WAREHOUSEMAN_ID=468845503
MANAGER_ID=460275491

# Настройки (опционально)
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

### Шаг 2: Убедитесь что миграции готовы

```bash
# Проверьте что миграции есть
alembic history

# Если миграций нет - создайте
alembic revision --autogenerate -m "Initial migration"
```

---

## 📦 ПОШАГОВАЯ ИНСТРУКЦИЯ: Hetzner Cloud

### 1. Создание сервера

1. Зайдите на https://console.hetzner.cloud
2. Нажмите **"New Project"** → назовите "Telegram Bot"
3. Нажмите **"Add Server"**
4. Выберите:
   - **Location:** Nürnberg (Германия)
   - **Image:** Ubuntu 22.04
   - **Type:** CX11 (1 CPU, 2GB RAM) - достаточно для бота
   - **SSH Key:** Добавьте ваш SSH ключ (или создайте новый)
5. Нажмите **"Create & Buy Now"**

### 2. Подключение к серверу

```bash
# Windows (PowerShell)
ssh root@ваш_IP_адрес

# Или через PuTTY (если нет SSH клиента)
```

### 3. Установка Python и зависимостей

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python 3.11
apt install -y python3.11 python3.11-venv python3-pip git

# Создание пользователя для бота (безопаснее чем root)
adduser botuser
usermod -aG sudo botuser
su - botuser

# Создание директории для проекта
mkdir -p ~/telegram-bot
cd ~/telegram-bot
```

### 4. Загрузка кода на сервер

**Вариант А: Через Git (если есть репозиторий)**
```bash
git clone https://github.com/ваш_username/ваш_репозиторий.git .
```

**Вариант Б: Через SCP (если нет Git)**
```bash
# На вашем компьютере (Windows PowerShell)
scp -r C:\Users\Автор\Documents\жас\* botuser@ваш_IP:~/telegram-bot/
```

**Вариант В: Ручная загрузка через SFTP**
- Используйте FileZilla или WinSCP
- Подключитесь к серверу
- Загрузите все файлы проекта

### 5. Установка зависимостей

```bash
cd ~/telegram-bot

# Создание виртуального окружения
python3.11 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt
```

### 6. Создание .env файла

```bash
nano .env
```

Вставьте переменные окружения (из Шага 1 выше):
```
BOT_TOKEN=...
DATABASE_URL=...
WAREHOUSEMAN_ID=...
MANAGER_ID=...
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 7. Применение миграций базы данных

```bash
source venv/bin/activate
alembic upgrade head
```

### 8. Тестовый запуск

```bash
source venv/bin/activate
python main.py
```

Если всё работает - остановите бота (`Ctrl+C`)

---

## 🔄 НАСТРОЙКА СИСТЕМНОГО СЕРВИСА (systemd)

Бот должен работать постоянно, даже после перезагрузки сервера.

### Создание сервисного файла

```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

Вставьте следующее:

```ini
[Unit]
Description=Telegram Bot "Завхоз"
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/telegram-bot
Environment="PATH=/home/botuser/telegram-bot/venv/bin"
ExecStart=/home/botuser/telegram-bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**ВАЖНО:** Замените пути на реальные:
- `/home/botuser/telegram-bot` → ваш путь к проекту
- `botuser` → ваш username

### Активация сервиса

```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Включение автозапуска
sudo systemctl enable telegram-bot.service

# Запуск бота
sudo systemctl start telegram-bot.service

# Проверка статуса
sudo systemctl status telegram-bot.service

# Просмотр логов
sudo journalctl -u telegram-bot.service -f
```

---

## ✅ ПРОВЕРКА РАБОТЫ

### 1. Проверка логов

```bash
sudo journalctl -u telegram-bot.service -n 50
```

Должны увидеть:
```
INFO - Запуск бота 'Завхоз'...
INFO - База данных инициализирована
INFO - Бот запущен
```

### 2. Тестирование в Telegram

1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Проверьте что меню появилось
4. Создайте тестовую заявку

---

## 🔧 УПРАВЛЕНИЕ БОТОМ

```bash
# Запуск
sudo systemctl start telegram-bot

# Остановка
sudo systemctl stop telegram-bot

# Перезапуск
sudo systemctl restart telegram-bot

# Просмотр статуса
sudo systemctl status telegram-bot

# Просмотр логов (последние 100 строк)
sudo journalctl -u telegram-bot -n 100

# Просмотр логов в реальном времени
sudo journalctl -u telegram-bot -f
```

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Бот не запускается

```bash
# Проверьте логи
sudo journalctl -u telegram-bot -n 50

# Проверьте права на файлы
ls -la /home/botuser/telegram-bot

# Проверьте .env файл
cat /home/botuser/telegram-bot/.env
```

### Ошибка подключения к базе данных

- Проверьте `DATABASE_URL` в `.env`
- Убедитесь что база данных Neon доступна извне
- Проверьте что IP сервера не заблокирован в Neon

### OpenAI API не работает

- Убедитесь что сервер в Европе
- Проверьте что OpenAI API ключ правильный

---

## 💰 РАСХОДЫ

- **VPS (Hetzner CX11):** €4.15/мес (~$4.50)
- **Neon PostgreSQL:** бесплатно (до 0.5 GB)
- **OpenAI API:** оплата по использованию (~$5-10/мес)
- **Итого:** ~€10-15/мес (~$11-16)

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ МАТЕРИАЛЫ

- **Hetzner Cloud Docs:** https://docs.hetzner.com/cloud/
- **systemd Guide:** https://www.freedesktop.org/software/systemd/man/systemd.service.html
- **aiogram Docs:** https://docs.aiogram.dev/

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Выберите провайдера VPS (рекомендую Hetzner)
2. ✅ Создайте сервер
3. ✅ Загрузите код
4. ✅ Настройте .env
5. ✅ Примените миграции
6. ✅ Настройте systemd
7. ✅ Протестируйте бота

**Готовы начать?** 🚀

