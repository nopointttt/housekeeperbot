# 🚀 Быстрая инструкция для деплоя на Ubuntu (стационарник)

## 🗄️ База данных

**ВАЖНО:** База данных - это **Neon PostgreSQL** (облачная база данных).

✅ **НЕ нужно устанавливать PostgreSQL на стационарный компьютер!**
✅ **НЕ нужно настраивать локальную базу данных!**

База данных находится в облаке (Neon.tech), нужно только:
- Строка подключения `DATABASE_URL` в файле `.env`
- **Интернет-соединение** на стационарном компьютере (для подключения к облачной БД)

---

## ✅ Что нужно для деплоя

1. **Ubuntu** установлен на стационарном компьютере
2. **Python 3.11** установлен
3. **Проект** скопирован на сервер
4. **Файл `.env`** создан с переменными окружения (включая `DATABASE_URL`)
5. **Интернет-соединение** (для подключения к облачной БД Neon)

---

## 📋 Пошаговая инструкция

### 1. Установка зависимостей

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python 3.11 (если нет)
sudo apt install -y python3.11 python3.11-venv python3-pip git

# Переход в директорию проекта
cd /path/to/your/project

# Создание виртуального окружения
python3.11 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt
```

### 2. Создание файла `.env`

```bash
nano .env
```

Вставьте:
```env
BOT_TOKEN=ваш_токен_от_BotFather
DATABASE_URL=postgresql+asyncpg://user:password@host.neon.tech/database?ssl=true
WAREHOUSEMAN_ID=468845503
MANAGER_ID=460275491
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

**Про `DATABASE_URL`:**
- Это строка подключения к **облачной базе данных Neon PostgreSQL**
- Формат: `postgresql+asyncpg://user:password@host.neon.tech/database?ssl=true`
- Получить можно в панели управления Neon.tech
- **НЕ нужно устанавливать PostgreSQL локально!**

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 3. Применение миграций БД

```bash
source venv/bin/activate
alembic upgrade head
```

### 4. Тестовый запуск

```bash
source venv/bin/activate
python main.py
```

Если всё работает - остановите (`Ctrl+C`)

---

## 🔄 Настройка автозапуска через systemd

### Создание сервисного файла

```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

Вставьте (замените пути на реальные):

```ini
[Unit]
Description=Telegram Bot "Завхоз"
After=network.target

[Service]
Type=simple
User=ваш_username
WorkingDirectory=/путь/к/проекту
Environment="PATH=/путь/к/проекту/venv/bin"
ExecStart=/путь/к/проекту/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Пример:**
```ini
[Unit]
Description=Telegram Bot "Завхоз"
After=network.target

[Service]
Type=simple
User=admin
WorkingDirectory=/home/admin/telegram-bot
Environment="PATH=/home/admin/telegram-bot/venv/bin"
ExecStart=/home/admin/telegram-bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Активация сервиса

```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Включение автозапуска при перезагрузке
sudo systemctl enable telegram-bot.service

# Запуск бота
sudo systemctl start telegram-bot.service

# Проверка статуса
sudo systemctl status telegram-bot.service
```

---

## ✅ Проверка работы

### Просмотр логов

```bash
# Последние 50 строк
sudo journalctl -u telegram-bot.service -n 50

# В реальном времени
sudo journalctl -u telegram-bot.service -f
```

Должны увидеть:
```
INFO - Запуск бота 'Завхоз'...
INFO - База данных инициализирована
INFO - Бот запущен
```

### Тестирование в Telegram

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Проверьте что меню появилось

---

## 🔧 Управление ботом

```bash
# Запуск
sudo systemctl start telegram-bot

# Остановка
sudo systemctl stop telegram-bot

# Перезапуск
sudo systemctl restart telegram-bot

# Статус
sudo systemctl status telegram-bot

# Логи (последние 100 строк)
sudo journalctl -u telegram-bot -n 100

# Логи в реальном времени
sudo journalctl -u telegram-bot -f
```

---

## ⚠️ Важные моменты

1. **Бот использует POLLING** (не webhook) - постоянно опрашивает Telegram API
2. **Компьютер должен быть включен** 24/7 для работы бота
3. **Автозапуск при перезагрузке** настроен через systemd
4. **Если компьютер выключится** - бот остановится, но запустится автоматически при включении

---

## 🆘 Решение проблем

### Бот не запускается

```bash
# Проверьте логи
sudo journalctl -u telegram-bot -n 50

# Проверьте права на файлы
ls -la /путь/к/проекту

# Проверьте .env файл
cat /путь/к/проекту/.env
```

### Ошибка подключения к БД

- Проверьте `DATABASE_URL` в `.env` (должен быть правильный формат)
- Убедитесь что база данных Neon доступна из интернета
- Проверьте **интернет-соединение** на стационарном компьютере
- Убедитесь что IP адрес компьютера не заблокирован в настройках Neon (если есть firewall)
- Проверьте что строка подключения содержит `?ssl=true` (для безопасного подключения)

---

**Готово! Бот будет работать постоянно, даже после перезагрузки компьютера.** 🚀

