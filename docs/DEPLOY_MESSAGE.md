# 📋 Инструкция для деплоя бота "Завхоз"

Привет! Это Telegram бот для автоматизации управления заявками и складом в организации.

## 🎯 Что это за проект

Telegram бот на Python (aiogram 3.x) для управления заявками сотрудников, складом и обработки жалоб. Бот работает с PostgreSQL базой данных.

## 🛠 Технический стек

- **Python 3.11+**
- **aiogram 3.13.1** - Telegram Bot Framework
- **PostgreSQL 16** - база данных
- **SQLAlchemy 2.0** - ORM
- **Alembic** - миграции БД
- **Docker & Docker Compose** - контейнеризация

## 📦 Что нужно для деплоя

1. **Docker** и **Docker Compose** (рекомендуется)
   - Или Python 3.11+ с виртуальным окружением
   
2. **PostgreSQL база данных**
   - В Docker Compose уже настроена автоматически
   - Или внешняя БД (Neon, Supabase и т.д.)

3. **Telegram Bot Token**
   - Получить у [@BotFather](https://t.me/BotFather) в Telegram

## 🚀 Быстрый старт (Docker - рекомендуется)

### 1. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```env
# Обязательные переменные
BOT_TOKEN=your_bot_token_from_botfather
WAREHOUSEMAN_ID=468845503
MANAGER_ID=460275491

# Опциональные (есть значения по умолчанию)
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

**Как узнать Telegram ID:**
- Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
- Отправьте `/start`
- Скопируйте ваш ID (число)

### 2. Запуск

```bash
# Запустить все сервисы (бот + PostgreSQL)
docker compose up -d

# Просмотр логов
docker compose logs -f bot

# Остановка
docker compose stop
```

### 3. Проверка

Откройте Telegram, найдите вашего бота и отправьте `/start`

## 📁 Структура проекта

```
├── main.py                 # Точка входа
├── bot/                    # Основной код бота
├── alembic/                # Миграции БД
├── tests/                  # Автотесты (301 тест)
├── docs/                   # Документация
├── docker-compose.yml      # Docker конфигурация
├── Dockerfile              # Образ бота
└── .env                    # Переменные окружения (создать из .env.example)
```

## ⚙️ Важные моменты

### База данных
- **В Docker**: PostgreSQL запускается автоматически, миграции применяются при старте
- **Внешняя БД**: Используйте формат `postgresql+asyncpg://user:pass@host:port/dbname`
- Миграции: `python -m alembic upgrade head` (в Docker делается автоматически)

### Логирование
- По умолчанию: `LOG_LEVEL=INFO` (production-ready)
- Для отладки: установите `LOG_LEVEL=DEBUG` в `.env`
- Логи выводятся в stdout (в Docker: `docker compose logs`)

### Порты
- PostgreSQL: `5432` (можно изменить в `docker-compose.yml`)
- Если порт занят, измените в `docker-compose.yml`:
  ```yaml
  ports:
    - "5433:5432"  # Используйте другой порт
  ```

## 📚 Документация

- **[DOCKER_DEPLOY.md](docs/deployment/DOCKER_DEPLOY.md)** - Полная инструкция по Docker деплою
- **[QUICK_START_DOCKER.md](docs/deployment/QUICK_START_DOCKER.md)** - Быстрый старт
- **[PRODUCTION_READY.md](PRODUCTION_READY.md)** - Отчет о готовности к production
- **[PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)** - Структура проекта

## ✅ Проверка готовности

Проект полностью готов к деплою:
- ✅ Все автотесты пройдены (301 тест)
- ✅ Настройки для production
- ✅ Docker конфигурация готова
- ✅ Миграции настроены

## 🐛 Решение проблем

### Бот не запускается
```bash
# Проверьте логи
docker compose logs bot

# Проверьте переменные окружения
docker compose exec bot env | grep BOT_TOKEN
```

### База данных не подключается
```bash
# Проверьте статус PostgreSQL
docker compose ps postgres

# Проверьте логи БД
docker compose logs postgres
```

### Порт занят
Измените порт в `docker-compose.yml` или остановите приложение, использующее порт 5432

## 🗑️ Очистка базы данных

Если нужно очистить базу данных от всех данных:

```bash
# Windows
scripts\clear_database.bat

# Linux/Mac
./scripts/clear_database.sh
```

**Внимание:** Это удалит ВСЕ данные! Подробнее: [`CLEAR_DATABASE.md`](CLEAR_DATABASE.md)

## 📞 Контакты

Если возникнут вопросы по деплою или настройке - обращайтесь к заказчику проекта.

---

**Удачи с деплоем! 🚀**

