# Вопросы по техническому заданию - Бот "Завхоз"

## 📋 ПРИНЯТЫЕ РЕШЕНИЯ (MVP подход)

**Цель:** Создать минимально жизнеспособный продукт (MVP) с простейшей реализацией
**Деплой:** Cloudflare Workers/Pages
**База данных:** Neon SQL (PostgreSQL)

---

## 🔍 Уточняющие вопросы для разработки

### 1. Технический стек

**1.1 Язык программирования и фреймворк:**
- ✅ **РЕШЕНО:** Python + aiogram 3.x
- ✅ **Обоснование:** Для MVP - проверенный фреймворк, хорошая документация, легко развернуть

**1.2 База данных:**
- ✅ **РЕШЕНО:** Neon SQL (PostgreSQL)
- ✅ **История изменений:** НЕТ (для MVP - только текущий статус в заявке)
- ✅ **Логирование действий:** НЕТ (для MVP - только базовые логи ошибок в консоль)

**1.3 Хранение файлов:**
- ✅ **РЕШЕНО:** Cloudflare R2 (объектное хранилище) или сохранение только ссылок на файлы Telegram
- ✅ **Максимальный размер фото:** 20 МБ (ограничение Telegram)
- ✅ **Ограничение объема:** НЕТ (для MVP, в будущем можно добавить)

---

### 2. Настройки и конфигурация

**2.1 Telegram ID:**
- ✅ **РЕШЕНО:** Переменные окружения (env файл) + возможность изменения через команду /settings для завхоза
- ✅ **Изменение ID:** ДА, через команду /settings (только для завхоза)
- ✅ **Несколько завхозов:** НЕТ (для MVP - только один)

**2.2 Часовой пояс:**
- ✅ **РЕШЕНО:** UTC+3 (Московское время) по умолчанию, настраивается через env переменную TIMEZONE
- ✅ **Поддержка разных поясов:** НЕТ (для MVP - один часовой пояс)

---

### 3. Функциональность заявок

**3.1 Нумерация заявок:**
- ✅ **РЕШЕНО:** Формат ЗХ-ДДММГГ-№№№, где ДДММГГ - день/месяц/год (27.11.25 → 271125)
- ✅ **Больше 999 заявок:** Использовать 4 цифры (0001-9999) или последовательность без ограничения
- ✅ **Ручное редактирование:** НЕТ (для MVP - только автоматическая нумерация)

**3.2 Категории:**
- ✅ **РЕШЕНО:** Список фиксированный (нельзя изменять через интерфейс, только в коде)
- ✅ **Категория "Прочее":** НЕТ дополнительного описания (для MVP)

**3.3 Определение материала:**
- ✅ **РЕШЕНО:** По категории. Категории-материалы: "Канцелярия", "Чай, кофе, сахар, вода", "Хозтовары и уборка", "ИТ-оборудование и расходники"
- ✅ **Для остальных категорий:** Количество не запрашивается

**3.4 Фото:**
- ✅ **Добавление после создания:** НЕТ (для MVP - только при создании)
- ✅ **Удаление фото:** НЕТ (для MVP)

**3.5 Статусы и история:**
- ✅ **История статусов:** НЕТ (для MVP - только текущий статус)
- ✅ **Откат статуса:** НЕТ (для MVP - нельзя изменить статус назад)

---

### 4. Склад

**4.1 Единицы измерения:**
- ✅ **РЕШЕНО:** Все в штуках (для MVP - проще всего)
- ✅ **Разные единицы:** НЕТ (для MVP)

**4.2 Приход товара:**
- ✅ **Добавление без заявки:** ДА (через меню "Склад" → "Добавить позицию")
- ✅ **Логирование операций:** НЕТ (для MVP - только текущее состояние)
- ✅ **Редактирование минимального остатка:** ДА (через кнопку "Изменить минимум")

**4.3 Напоминания:**
- ✅ **РЕШЕНО:** Каждый день в 8:30 (для MVP - проще реализовать)
- ✅ **Несколько позиций:** Одно сообщение со списком всех позиций
- ✅ **Отключение напоминаний:** НЕТ (для MVP)

---

### 5. Рассылки

**5.1 Рассылка всем сотрудникам:**
- ✅ **РЕШЕНО:** Только текст (для MVP - самый простой вариант)
- ✅ **Рассылку по отделам:** НЕТ (для MVP - только всем)
- ✅ **Подтверждение:** ДА (показать превью и кнопку "Отправить" / "Отмена")

---

### 6. Жалобы

**6.1 Причины жалоб:**
- ✅ **РЕШЕНО:** Фиксированный список: "Задержка выполнения", "Невыполнение заявки", "Грубое обращение", "Другое"
- ✅ **Свои причины:** НЕТ (для MVP - только фиксированный список)

**6.2 Обработка жалоб:**
- ✅ **Ответ руководителя:** НЕТ (для MVP - только просмотр)
- ✅ **Уведомление об обработке:** НЕТ (для MVP)
- ✅ **Статистика по жалобам:** НЕТ (для MVP)

---

### 7. Отчеты

**7.1 Отчёт за период:**
- ✅ **РЕШЕНО:** Минимум метрик (для MVP): количество новых, выполненных, отклонённых, срочных
- ✅ **Экспорт:** НЕТ (для MVP - только текст в Telegram)

**7.2 Автоматические отчёты:**
- ✅ **Ежедневный отчёт:** Только за предыдущий день (для MVP)
- ✅ **Еженедельные/месячные:** НЕТ (для MVP)

---

### 8. Интерфейс и UX

**8.1 Уведомления:**
- ✅ **РЕШЕНО:** Уведомления о смене статуса заявки, ответ завхоза (через кнопку "Написать сотруднику")
- ✅ **Отключение уведомлений:** НЕТ (для MVP - все получают уведомления)

**8.2 Поиск и фильтры:**
- ✅ **Поиск:** НЕТ (для MVP - только просмотр списков)
- ✅ **Фильтры:** НЕТ (для MVP - только стандартные списки)

---

### 9. Безопасность

**9.1 Доступ:**
- ✅ **Блокировка пользователей:** НЕТ (для MVP)
- ✅ **Возврат пользователя:** ДА, заявки сохраняются (по Telegram ID)

**9.2 Данные:**
- ✅ **Шифрование:** НЕТ (для MVP - стандартная защита Neon SQL)
- ✅ **Хранение заявок:** Бессрочно (для MVP - без ограничений)
- ✅ **Удаление старых заявок:** НЕТ (для MVP - все хранятся)

---

### 10. Развертывание и инфраструктура

**10.1 Хостинг:**
- ✅ **РЕШЕНО:** Cloudflare Workers/Pages (webhook режим для Telegram)
- ✅ **Альтернатива:** Если Workers не подойдёт - Railway.app или Render (бесплатный тариф для MVP)

**10.2 Резервное копирование:**
- ✅ **Автоматические бэкапы:** НЕТ (для MVP - Neon SQL имеет встроенные бэкапы)
- ✅ **Ручные бэкапы:** Можно делать через Neon Dashboard при необходимости

**10.3 Мониторинг:**
- ✅ **Мониторинг:** НЕТ (для MVP - только логи ошибок в консоль Cloudflare)
- ✅ **Логи ошибок:** Да, базовое логирование в stdout/stderr (Cloudflare автоматически логирует)

---

### 11. Дополнительные функции (на будущее)

**11.1 Интеграции:**
- ✅ **Интеграции:** НЕТ (для MVP - только Telegram бот)
- ✅ **Веб-панель:** НЕТ (для MVP - только Telegram интерфейс)

**11.2 Расширение:**
- ✅ **Другие роли:** НЕТ (для MVP - только 3 роли из ТЗ)
- ✅ **Мобильная версия/веб:** НЕТ (для MVP - Telegram работает везде)

---

## ✅ Приоритетные вопросы (ответы критичны для начала разработки)

1. ✅ **Технологический стек** - Python + aiogram 3.x
2. ✅ **База данных** - Neon SQL (PostgreSQL)
3. ✅ **Настройка Telegram ID** - переменные окружения + команда /settings
4. ✅ **Хранение фото** - Cloudflare R2 или ссылки на файлы Telegram
5. ✅ **Часовой пояс** - UTC+3 (настраивается через env)

---

## 📝 ИТОГОВЫЕ РЕШЕНИЯ ДЛЯ MVP

### Технологии:
- **Язык:** Python 3.11+
- **Фреймворк:** aiogram 3.x
- **База данных:** Neon SQL (PostgreSQL)
- **Хранилище файлов:** Cloudflare R2 (или ссылки на Telegram файлы)
- **Деплой:** Cloudflare Workers/Pages (webhook) или Railway.app/Render (fallback)
- **Планировщик задач:** Cloudflare Cron Triggers (для автоматических уведомлений)

### Упрощения для MVP:
- Фиксированные категории (не редактируются)
- Минимум метрик в отчётах
- Нет истории изменений
- Простая система склада (только штуки)
- Только текст в рассылках
- Один часовой пояс
- Нет блокировки пользователей
- Нет поиска и фильтров
- Все уведомления включены

### Следующий шаг:
Переход в **PLAN режим** для детального планирования архитектуры и структуры проекта.

---

*Все решения приняты с фокусом на MVP - минимально жизнеспособный продукт. Расширение функционала возможно после запуска.*

