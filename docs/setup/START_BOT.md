# 🚀 Команды для запуска бота

## 📡 Режим работы: Polling (Long Polling)

Бот использует **polling** (long polling) - постоянно опрашивает Telegram API на наличие новых сообщений.

**Преимущества:**
- ✅ Простая настройка (не нужен веб-сервер)
- ✅ Не нужен SSL сертификат
- ✅ Работает за NAT/файрволом
- ✅ Подходит для большинства ботов

**Для продакшена на VPS:** бот запускается как системный сервис через systemd (см. `DEPLOY_VPS_GUIDE.md`)

---

## 📍 Windows (PowerShell)

### 1. Активация виртуального окружения и запуск

```powershell
# Перейти в директорию проекта
cd C:\Users\Автор\Documents\жас

# Активировать виртуальное окружение
.\venv\Scripts\Activate.ps1

# Запустить бота
python main.py
```

### 2. Одной строкой (если уже в директории проекта)

```powershell
.\venv\Scripts\Activate.ps1; python main.py
```

---

## 📍 Linux/Mac (Bash)

```bash
# Перейти в директорию проекта
cd ~/telegram-bot

# Активировать виртуальное окружение
source venv/bin/activate

# Запустить бота
python main.py
```

---

## ✅ Перед запуском убедитесь:

1. ✅ **Файл `.env` существует** и заполнен:
   - `BOT_TOKEN=...`
   - `DATABASE_URL=...`
   - `WAREHOUSEMAN_ID=...`
   - `MANAGER_ID=...`

2. ✅ **Миграции применены**:
   ```powershell
   alembic upgrade head
   ```

3. ✅ **Зависимости установлены**:
   ```powershell
   pip install -r requirements.txt
   ```

---

## 🛑 Остановка бота

Нажмите `Ctrl+C` в терминале

---

## 📋 Если возникли ошибки

### Ошибка: "BOT_TOKEN не установлен"
- Проверьте файл `.env` существует
- Проверьте что переменные заполнены

### Ошибка: "Database connection failed"
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь что база данных Neon доступна

### Ошибка: "ModuleNotFoundError"
- Активируйте виртуальное окружение
- Установите зависимости: `pip install -r requirements.txt`

