# 🚀 Быстрый запуск бота

## 📋 Чеклист перед запуском

### 1. ✅ Проверка .env файла

Убедитесь, что в `.env` файле заполнены все необходимые переменные:

```env
# Обязательные переменные
BOT_TOKEN=your_bot_token_from_botfather
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/database
WAREHOUSEMAN_ID=468845503
MANAGER_ID=460275491

# Опционально - список ID сотрудников через запятую
# Если не указано, доступ будет проверяться через БД (таблица allowed_users)
ALLOWED_EMPLOYEE_IDS=123456789,987654321

# Опциональные переменные (есть значения по умолчанию)
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

### 2. ✅ Применить миграции

Проверьте и примените миграции базы данных:

```bash
# Проверить текущее состояние миграций
python -m alembic current

# Применить все миграции
python -m alembic upgrade head
```

**Важно:** Новая миграция создаст таблицу `allowed_users` для белого списка сотрудников.

### 3. ✅ Добавить пользователей (опционально)

**Вариант A: Через конфиг (.env)**
- Добавьте ID в `ALLOWED_EMPLOYEE_IDS`
- Перезапустите бота

**Вариант B: Через базу данных**
```sql
INSERT INTO allowed_users (id, full_name) 
VALUES 
  (123456789, 'Иванов Иван Иванович'),
  (987654321, 'Петров Петр Петрович');
```

**Вариант C: Использовать скрипт синхронизации**
```bash
python scripts/sync_allowed_users.py
```

### 4. ✅ Запустить бота

```bash
python main.py
```

## 🔍 Проверка работы

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Проверьте что:
   - Бот отвечает приветственным сообщением
   - Показывается правильная клавиатура в зависимости от роли
   - Если пользователь не в белом списке - показывается сообщение "У вас нет доступа"

## ⚠️ Частые проблемы

### Ошибка: "BOT_TOKEN не установлен"
- Проверьте что файл `.env` существует и содержит `BOT_TOKEN`
- Убедитесь что файл находится в корневой директории проекта

### Ошибка: "DATABASE_URL не установлен"
- Для Docker: `DATABASE_URL` настраивается автоматически
- Для локального запуска: укажите строку подключения к PostgreSQL

### Ошибка: "Таблица allowed_users не существует"
- Примените миграции: `python -m alembic upgrade head`

### Ошибка: "У вас нет доступа к этому боту"
- Добавьте ваш Telegram ID в `ALLOWED_EMPLOYEE_IDS` в `.env`
- Или добавьте в БД: `INSERT INTO allowed_users (id, full_name) VALUES (YOUR_ID, 'Ваше имя');`

## 📚 Дополнительная информация

- [Полная документация по конфигурации](CONFIG_EMPLOYEES.md)
- [Docker деплой](deployment/DOCKER_DEPLOY.md)
- [Структура проекта](../PROJECT_STRUCTURE.md)

