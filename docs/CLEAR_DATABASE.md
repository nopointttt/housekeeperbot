# 🗑️ Очистка базы данных

Инструкция по полной очистке базы данных от всех данных.

## ⚠️ Внимание!

**Это действие удалит ВСЕ данные из базы данных:**
- Всех пользователей
- Все заявки
- Все фотографии
- Все жалобы
- Все товары на складе

**Это действие необратимо!** Убедитесь, что у вас есть резервная копия, если данные важны.

## 🚀 Способы очистки

### Способ 1: Скрипт (Рекомендуется)

#### Windows

**В Command Prompt (cmd):**
```cmd
scripts\clear_database.bat
```

**В PowerShell:**
```powershell
# Вариант 1: Запуск .bat файла
.\scripts\clear_database.bat

# Вариант 2: Запуск PowerShell скрипта (рекомендуется)
.\scripts\clear_database.ps1
```

#### Linux/Mac
```bash
chmod +x scripts/clear_database.sh
./scripts/clear_database.sh
```

#### Вручную через Python
```bash
python scripts/clear_database.py
```

### Способ 2: Через Docker

```bash
# Остановить контейнеры
docker compose down

# Удалить volumes (удалит все данные БД)
docker compose down -v

# Запустить заново
docker compose up -d
```

### Способ 3: Через SQL

Подключитесь к базе данных и выполните:

```sql
-- Удалить все данные
DELETE FROM request_photos;
DELETE FROM complaints;
DELETE FROM requests;
DELETE FROM warehouse_items;
DELETE FROM users;

-- Сбросить счетчики (PostgreSQL)
ALTER SEQUENCE requests_id_seq RESTART WITH 1;
ALTER SEQUENCE request_photos_id_seq RESTART WITH 1;
ALTER SEQUENCE warehouse_items_id_seq RESTART WITH 1;
ALTER SEQUENCE complaints_id_seq RESTART WITH 1;
```

## ✅ Проверка

После очистки проверьте, что база пуста:

```bash
# Через Docker
docker compose exec postgres psql -U zavhoz -d zavhoz_db -c "SELECT COUNT(*) FROM users;"
docker compose exec postgres psql -U zavhoz -d zavhoz_db -c "SELECT COUNT(*) FROM requests;"
```

Ожидаемый результат: `0` для всех таблиц.

## 📝 После очистки

После очистки базы данных:
1. Бот автоматически создаст новых пользователей при первом использовании
2. Все счетчики автоинкремента сброшены
3. База готова к работе с чистого листа

---

**Дата создания:** 2025-12-09

