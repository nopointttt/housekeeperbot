# Промпт для Max Mode: Создание полной системы автоматического тестирования

## 🎯 Задача
Создать полноценную систему автоматического тестирования для Telegram бота "Завхоз" с покрытием всех компонентов: handlers, services, FSM states, database operations, и end-to-end сценариев.

## 📋 Что нужно сделать

### 1. Подготовка инфраструктуры тестирования

#### 1.1. Обновить requirements.txt
Добавить зависимости для тестирования:
- `pytest==8.0.0` - основной фреймворк тестирования
- `pytest-asyncio==0.23.0` - поддержка async тестов
- `pytest-mock==3.12.0` - моки и стабы
- `pytest-cov==4.1.0` - покрытие кода
- `faker==22.0.0` - генерация тестовых данных
- `pytest-postgresql==5.0.0` или использовать SQLite в памяти для тестов
- `httpx==0.26.0` - для моков HTTP запросов (если нужно)

#### 1.2. Создать структуру папок tests/
```
tests/
├── __init__.py
├── conftest.py              # Общие fixtures для всех тестов
├── fixtures/                # Тестовые данные
│   ├── __init__.py
│   ├── database.py          # Fixtures для БД
│   ├── bot.py               # Fixtures для Bot/Dispatcher
│   └── data.py              # Тестовые данные (пользователи, заявки)
├── unit/                    # Unit тесты
│   ├── __init__.py
│   ├── test_services/
│   │   ├── __init__.py
│   │   ├── test_request_service.py
│   │   ├── test_role_service.py
│   │   ├── test_warehouse_service.py
│   │   ├── test_complaint_service.py
│   │   ├── test_broadcast_service.py
│   │   └── test_notification_service.py
│   ├── test_utils/
│   │   ├── __init__.py
│   │   ├── test_request_formatter.py
│   │   └── test_request_helpers.py
│   └── test_models/
│       ├── __init__.py
│       └── test_database_models.py
├── integration/             # Интеграционные тесты
│   ├── __init__.py
│   ├── test_handlers/
│   │   ├── __init__.py
│   │   ├── test_start_handler.py
│   │   ├── test_request_creation_flow.py
│   │   ├── test_employee_handlers.py
│   │   ├── test_warehouseman_handlers.py
│   │   └── test_manager_handlers.py
│   ├── test_fsm/
│   │   ├── __init__.py
│   │   ├── test_request_creation_fsm.py
│   │   └── test_complaint_fsm.py
│   ├── test_middleware/
│   │   ├── __init__.py
│   │   └── test_role_middleware.py
│   └── test_database/
│       ├── __init__.py
│       ├── test_crud_operations.py
│       └── test_relationships.py
└── e2e/                     # End-to-end тесты
    ├── __init__.py
    ├── test_scenarios/
    │   ├── __init__.py
    │   ├── test_create_request_scenario.py
    │   ├── test_request_workflow_scenario.py
    │   └── test_complaint_scenario.py
    └── test_full_flows.py
```

### 2. Создать conftest.py с общими fixtures

#### 2.1. Fixtures для БД
- `test_db_session` - тестовая сессия БД с автоматическим откатом транзакций
- `test_db_engine` - тестовый engine (SQLite в памяти или отдельная тестовая PostgreSQL)
- `test_db_base` - применение миграций для тестовой БД

#### 2.2. Fixtures для Telegram Bot
- `mock_bot` - мок объекта Bot из aiogram
- `mock_dispatcher` - мок Dispatcher
- `mock_message` - мок Message объекта
- `mock_callback_query` - мок CallbackQuery
- `mock_user` - мок пользователя Telegram

#### 2.3. Fixtures для тестовых данных
- `test_user` - тестовый пользователь
- `test_employee` - тестовый сотрудник
- `test_warehouseman` - тестовый завхоз
- `test_manager` - тестовый руководитель
- `test_request` - тестовая заявка
- `test_warehouse_item` - тестовый товар на складе

### 3. Unit тесты для сервисов

#### 3.1. test_request_service.py
Тестировать:
- `generate_request_number()` - генерация уникальных номеров
- `create_request()` - создание заявки с фото и без
- `get_user_requests()` - получение заявок пользователя
- `get_request_by_id()` - получение заявки по ID
- Edge cases: пустые данные, некорректные значения

#### 3.2. test_role_service.py
Тестировать:
- `get_or_create_user()` - создание/получение пользователя
- `determine_role()` - определение роли по ID
- `get_role_by_id()` - получение роли из конфига

#### 3.3. test_warehouse_service.py
Тестировать:
- CRUD операции со складом
- Проверка минимума
- Списание товара

#### 3.4. test_notification_service.py
Тестировать:
- Моки отправки сообщений
- Форматирование уведомлений

### 4. Интеграционные тесты для handlers

#### 4.1. test_request_creation_flow.py
Полный flow создания заявки:
1. Начало создания заявки
2. Выбор категории
3. Ввод описания
4. Ввод количества
5. Выбор приоритета
6. Загрузка фото (опционально)
7. Подтверждение
8. Проверка создания в БД

#### 4.2. test_employee_handlers.py
- Просмотр своих заявок
- Детали заявки
- Связь с завхозом

#### 4.3. test_warehouseman_handlers.py
- Просмотр новых заявок
- Смена статуса заявки
- Отклонение заявки
- Управление складом

### 5. Тесты FSM состояний

#### 5.1. test_request_creation_fsm.py
- Переходы между состояниями
- Валидация данных на каждом шаге
- Отмена создания заявки
- Обработка невалидных данных

### 6. End-to-end тесты сценариев

#### 6.1. test_create_request_scenario.py
Полный сценарий:
1. Сотрудник создает заявку
2. Заявка появляется у завхоза
3. Завхоз меняет статус
4. Сотрудник получает уведомление

#### 6.2. test_request_workflow_scenario.py
- Создание → В работе → Выполнено
- Создание → Отклонено

### 7. Настройка pytest.ini
Создать файл `pytest.ini` с конфигурацией:
- Пути к тестам
- Маркеры тестов
- Настройки asyncio
- Настройки покрытия

### 8. Скрипт запуска тестов
Создать `run_tests.py` или `scripts/test.sh` для:
- Запуска всех тестов
- Запуска с покрытием
- Запуска только unit/integration/e2e
- Генерации отчетов

### 9. GitHub Actions / CI конфигурация
Создать `.github/workflows/tests.yml` для автоматического запуска тестов

## 🔧 Требования к реализации

### Обязательно:
1. Все тесты должны быть изолированными (не зависеть друг от друга)
2. Использовать fixtures для общих настроек
3. Каждый тест должен очищать данные после выполнения
4. Тесты БД должны использовать транзакции с откатом
5. Моки для Telegram API должны быть реалистичными
6. Покрытие кода должно быть минимум 70%

### Важно:
- Все async функции должны использовать `pytest.mark.asyncio`
- Использовать `pytest.fixture` с `scope="function"` для изоляции
- Тестовые данные через `faker` для разнообразия
- Обработка ошибок должна тестироваться

### Структура каждого теста:
```python
async def test_function_name():
    # Arrange - подготовка данных
    # Act - выполнение действия
    # Assert - проверка результата
```

## 📊 Критерии успеха

1. ✅ Все тесты проходят успешно
2. ✅ Покрытие кода > 70%
3. ✅ Все критичные сценарии покрыты тестами
4. ✅ Тесты запускаются быстро (< 2 минут для всех)
5. ✅ Нет дублирования кода в тестах
6. ✅ Тесты легко читаются и понимаются

## 🚀 Дополнительно

Создать:
- `TESTING_GUIDE.md` - руководство по запуску тестов
- `.coveragerc` - конфигурация покрытия
- `scripts/test.sh` / `scripts/test.bat` - скрипты запуска
- Примеры запуска в README

