# Telegram Bot "Завхоз"

Бот для автоматизации управления заявками и складом в организации.

## 🚀 Технологии

- **Python 3.11+**
- **aiogram 3.x** - Telegram Bot API
- **PostgreSQL** - база данных
- **SQLAlchemy 2.0** - ORM
- **Alembic** - миграции
- **Docker** - контейнеризация

## 📋 Функционал

### Для сотрудников:
- Создание заявок через пошаговый мастер
- Просмотр своих заявок
- Связь с завхозом
- Подача жалоб

### Для завхоза:
- Управление заявками (статусы, действия)
- Управление складом
- Рассылки сотрудникам
- Настройки

### Для руководителя:
- Просмотр заявок и отчетов
- Просмотр жалоб
- Автоматические уведомления

## 🛠️ Установка

### Вариант 1: Docker (Рекомендуется)

1. Установите [Docker](https://www.docker.com/products/docker-desktop/)
2. Скопируйте `.env.example` в `.env` и заполните переменные
3. Запустите:
```bash
docker compose up -d
```

Подробная инструкция: [`docs/deployment/DOCKER_DEPLOY.md`](docs/deployment/DOCKER_DEPLOY.md)  
Быстрый старт: [`docs/deployment/QUICK_START_DOCKER.md`](docs/deployment/QUICK_START_DOCKER.md)

### Вариант 2: Локальная установка

1. Клонировать репозиторий
2. Создать виртуальное окружение:
```bash
python -m venv venv
```

3. Активировать виртуальное окружение:
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

4. Установить зависимости:
```bash
pip install -r requirements.txt
```

5. Настроить `.env` файл (см. `.env.example`)

6. Применить миграции:
```bash
alembic upgrade head
```

7. Запустить бота:
```bash
python main.py
```

## 📝 Конфигурация

Создайте файл `.env` на основе `.env.example`:

```env
BOT_TOKEN=your_telegram_bot_token
DATABASE_URL=postgresql+asyncpg://zavhoz:zavhoz_password@postgres:5432/zavhoz_db
WAREHOUSEMAN_ID=468845503
MANAGER_ID=460275491
ALLOWED_EMPLOYEE_IDS=123456789,987654321,555555555
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

**Переменные окружения:**
- `BOT_TOKEN` - токен бота от @BotFather (обязательно)
- `DATABASE_URL` - строка подключения к PostgreSQL (обязательно, в Docker настраивается автоматически)
- `WAREHOUSEMAN_ID` - Telegram ID завхоза (обязательно)
- `MANAGER_ID` - Telegram ID руководителя (обязательно)
- `ALLOWED_EMPLOYEE_IDS` - список ID сотрудников через запятую (опционально)
- `TIMEZONE` - часовой пояс (по умолчанию: Europe/Moscow)
- `LOG_LEVEL` - уровень логирования (по умолчанию: INFO)

**Примечание:** 
- При использовании Docker, `DATABASE_URL` настраивается автоматически
- `ALLOWED_EMPLOYEE_IDS` - список ID сотрудников с доступом к боту (через запятую, без пробелов). Если не указан, доступ проверяется через БД (таблица `allowed_users`)
- Завхоз и руководитель всегда имеют доступ, независимо от белого списка
- Подробнее о конфигурации: [`docs/CONFIG_EMPLOYEES.md`](docs/CONFIG_EMPLOYEES.md)

## 📚 Документация

- **[Структура проекта](PROJECT_STRUCTURE.md)** - Описание структуры проекта
- **[Документация проекта](docs/README.md)** - Полный список документации
- **[Очистка базы данных](CLEAR_DATABASE.md)** - Инструкция по очистке БД
- **Деплой:** [`docs/deployment/`](docs/deployment/) - Инструкции по деплою
- **Тестирование:** [`docs/testing/`](docs/testing/) - Руководство по тестированию
- **Настройка:** [`docs/setup/`](docs/setup/) - Инструкции по настройке
- **Разработка:** [`docs/development/`](docs/development/) - Документация для разработчиков
- **Спецификации:** [`specs/`](specs/) - Техническое задание
- **Memory Bank:** [`memory-bank/`](memory-bank/) - Внутренняя документация проекта

