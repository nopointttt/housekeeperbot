# 🚀 Быстрый деплой бота "Завхоз"

Краткая инструкция для быстрого развертывания бота через Docker.

## 📋 Шаг 1: Подготовка

1. Убедитесь, что установлен Docker:
```bash
docker --version
docker compose version
```

2. Скопируйте `.env.example` в `.env`:
```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

## 📝 Шаг 2: Настройка `.env`

Откройте файл `.env` и заполните:

```env
# Токен бота (получить у @BotFather)
BOT_TOKEN=ваш_токен_здесь

# ID завхоза (получить через @userinfobot)
WAREHOUSEMAN_ID=ваш_id_завхоза

# ID руководителя
MANAGER_ID=ваш_id_руководителя

# ID сотрудников (через запятую, без пробелов)
ALLOWED_EMPLOYEE_IDS=123456789,987654321

# Остальное можно оставить по умолчанию
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

**Как получить токен:**
- Откройте [@BotFather](https://t.me/BotFather) в Telegram
- Отправьте `/newbot` или выберите существующего бота
- Скопируйте токен

**Как узнать ID:**
- Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
- Отправьте `/start`
- Скопируйте ваш ID (число)

## 🐳 Шаг 3: Запуск

```bash
# Запуск в фоновом режиме
docker compose up -d

# Проверка статуса
docker compose ps

# Просмотр логов
docker compose logs -f bot
```

## ✅ Шаг 4: Проверка

1. Проверьте, что контейнеры запущены:
```bash
docker compose ps
```

Должны быть в статусе "Up":
- `zavhoz_postgres` (база данных)
- `zavhoz_bot` (бот)

2. Проверьте логи на ошибки:
```bash
docker compose logs bot | tail -50
```

3. Протестируйте бота в Telegram:
- Откройте вашего бота
- Отправьте `/start`
- Убедитесь, что бот отвечает

## 🔧 Полезные команды

```bash
# Остановить
docker compose down

# Перезапустить
docker compose restart bot

# Посмотреть логи
docker compose logs -f bot

# Остановить и удалить данные
docker compose down -v
```

## ❌ Решение проблем

### Бот не отвечает
1. Проверьте токен в `.env`
2. Проверьте логи: `docker compose logs bot`
3. Проверьте, что контейнер запущен: `docker compose ps`

### База данных не работает
1. Проверьте логи: `docker compose logs postgres`
2. Перезапустите: `docker compose restart postgres`

### Миграции не применены
1. Проверьте логи: `docker compose logs bot | grep alembic`
2. Примените вручную:
```bash
docker exec -it zavhoz_bot python -m alembic upgrade head
```

## 📚 Дополнительная документация

- Полная инструкция: [`docs/deployment/DOCKER_DEPLOY.md`](docs/deployment/DOCKER_DEPLOY.md)
- Чеклист деплоя: [`DEPLOYMENT_CHECKLIST.md`](DEPLOYMENT_CHECKLIST.md)
- Конфигурация сотрудников: [`docs/CONFIG_EMPLOYEES.md`](docs/CONFIG_EMPLOYEES.md)

## ✅ Готово!

Бот должен быть запущен и готов к работе. Удачи! 🎉

