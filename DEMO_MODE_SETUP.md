# 🚀 Инструкция: Запуск в DEMO-режиме на локалке

## Что такое DEMO-режим?

**DEMO_MODE=true** включает:
- ✅ **Публичный доступ** - любой пользователь может зайти в бота (без allowlist)
- ✅ **Изоляция данных** - каждый пользователь видит только свои данные (заявки/склад/жалобы)
- ✅ **Роль "Руководитель"** - все тестировщики могут переключаться между режимами (пользователь/техник/руководитель)
- ✅ **Управление техниками** - руководитель может назначать техников, которые будут видеть его заявки и склад
- ✅ **Ограничение 7 дней** - тестовый доступ автоматически блокируется через 7 дней с момента первого входа
- ✅ **Безопасность** - уведомления в демо не отправляются реальным ID техника/руководителя

## 📝 Шаг 1: Настройка .env

Добавь в свой `.env` файл:

```env
# Включить демо-режим
DEMO_MODE=true

# Опционально: ссылка на бота для маркетинга
BOT_PUBLIC_URL=@housekeeperrrrrbot
```

**Важно:**
- `WAREHOUSEMAN_ID` и `MANAGER_ID` в демо-режиме **не используются** для проверки доступа (любой может войти)
- Но они **нужны** для корректной работы кода (можешь оставить любые значения)
- `ALLOWED_EMPLOYEE_IDS` в демо-режиме **игнорируется** (можно оставить пустым)

**Пример полного .env для демо:**

```env
BOT_TOKEN=твой_токен_бота
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/dbname
WAREHOUSEMAN_ID=123456789
MANAGER_ID=987654321
ALLOWED_EMPLOYEE_IDS=
DEMO_MODE=true
BOT_PUBLIC_URL=@housekeeperrrrrbot
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

## 📦 Шаг 2: Применить миграции

В демо-режиме используется изоляция данных через `tenant_id`. Нужно применить миграцию:

```bash
# Windows (PowerShell)
.\venv\Scripts\python.exe -m alembic upgrade head

# Linux/Mac
python -m alembic upgrade head
```

Миграции добавят:
- Поле `tenant_id` в таблицы `requests`, `complaints`, `warehouse_items`, `users`
- Таблицу `user_events` для маркетинговых данных
- Таблицу `technician_assignments` для управления техниками

## ▶️ Шаг 3: Запустить бота

```bash
# Windows (PowerShell)
.\venv\Scripts\python.exe main.py

# Linux/Mac
python main.py
```

## ✅ Проверка

1. Зайди в бота через `/start` с **любого** Telegram аккаунта
2. Ты должен увидеть приветствие с информацией о тестовом периоде (7 дней) и клавиатуру руководителя
3. Попробуй переключиться на "Зайти как пользователь" или "Зайти как техник"
4. Создай тестовую заявку - она будет видна только тебе (изоляция)
5. Попробуй добавить техника через "Управление техниками" (нужен Telegram ID или username)

## 🔍 Как это работает

### Изоляция данных (tenant_id)

- При первом `/start` каждому пользователю назначается `tenant_id = его Telegram ID`
- Все заявки, жалобы и склад привязаны к `tenant_id`
- Пользователь видит **только свои** данные, даже если переключился на роль "техника" или "руководителя"

### Управление техниками

- Руководитель может добавить техника через "Управление техниками"
- Для добавления нужен Telegram ID или username техника (можно узнать через @userinfobot)
- Техник видит заявки и склад руководителя (используется tenant_id руководителя)
- Руководитель может удалить техника в любой момент

### Ограничение тестового доступа (7 дней)

- С момента первого `/start` у пользователя есть 7 дней бесплатного тестового доступа
- Оставшееся время отображается в приветственном сообщении
- После истечения срока доступ блокируется автоматически
- Пользователь видит сообщение "Тестовый доступ истек" при попытке использовать бота

### Маркетинговые данные

При каждом `/start` сохраняется:
- Профиль пользователя (имя, username, язык, премиум статус)
- Событие в таблице `user_events` (тип `start`, дата/время, параметры)

Это позволит в будущем анализировать:
- Кто и когда заходил в бота
- Откуда пришли пользователи (start_payload)
- Географию пользователей (если добавить в будущем)

## 🚫 Отключение демо-режима

Чтобы вернуться к обычному режиму (только разрешенные пользователи):

```env
DEMO_MODE=false
# или просто удали эту строку из .env
```

После отключения демо:
- Доступ будут иметь только пользователи из `ALLOWED_EMPLOYEE_IDS`
- `WAREHOUSEMAN_ID` и `MANAGER_ID` снова используются для проверки доступа
- Изоляция данных остается (но только для новых пользователей)

## ⚠️ Важно для продакшена

**НЕ включай DEMO_MODE на проде!**

Для продакшена используй:
- `DEMO_MODE=false` (или не указывай)
- `ALLOWED_EMPLOYEE_IDS` с реальными ID сотрудников
- Реальные `WAREHOUSEMAN_ID` и `MANAGER_ID`

